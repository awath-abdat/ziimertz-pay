import 'package:flutter/material.dart';

enum TopUpMethodPage { topupmethod }

class Constants {
  static String currency = "\$";
  static String appName = "Ziimertz Pay";

  static Locale engLocal = Locale('en');
  static Locale arLocal = Locale('ar');
}
